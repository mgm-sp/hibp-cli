complete -c hibp -n "__fish_use_subcommand" -f -a '(hibp commands)' -d 'subcommand'

complete -c hibp -n "contains request (commandline -poc)" -f -a "breachedaccount breacheddomain subscribeddomains breaches breach latestbreach dataclasses pasteaccount subscription stealerlogsbyemail stealerlogsbywebsitedomain stealerlogsbyemaildomain" -d "request"
